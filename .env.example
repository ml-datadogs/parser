# Bright Data residential proxy
BRIGHTDATA_CUSTOMER=
BRIGHTDATA_ZONE=
BRIGHTDATA_PASSWORD=
BRIGHTDATA_HOST=brd.superproxy.io
BRIGHTDATA_PORT=33335
BRIGHTDATA_COUNTRY=
# Set to true after installing Bright Data SSL certificate
BRIGHTDATA_VERIFY_SSL=false
# Bright Data Web Unlocker (anti-bot). Set the API token to route sites with an
# unlocker_zone (e.g. litfund) through the Web Unlocker API instead of the
# residential proxy. Leave empty to disable and use residential only.
BRIGHTDATA_API_TOKEN=
BRIGHTDATA_API_URL=https://api.brightdata.com/request
# In-flight request cap applied automatically when a site routes through the Web
# Unlocker. PAYG Unlocker concurrency is unlimited, so tune this against local
# resources and cost (not a zone limit). Default 128.
BRIGHTDATA_UNLOCKER_CONCURRENCY=128

# Resumable crawls: set to a directory to persist the request queue so an
# interrupted run resumes instead of refetching (and, under the per-request
# billed Unlocker, re-paying for) pages. Empty disables it.
JOBDIR=

# ClickHouse (optional; pipeline/worker disabled when host is empty)
CLICKHOUSE_HOST=
CLICKHOUSE_PORT=8123
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=
CLICKHOUSE_DATABASE=scraper
CLICKHOUSE_RAW_TABLE=raw_items
CLICKHOUSE_PARSED_TABLE=parsed_items
CLICKHOUSE_STATE_TABLE=parse_state
CLICKHOUSE_BATCH_SIZE=1000

# Parse worker
PARSE_BATCH_SIZE=1000

# Local file export
OUTPUT_DIR=output
